export interface TextStyle {
  fontFamily: string;
  fontSize: number;
  color: string;
}

export interface CSVMapping {
  customerName: string;
  phoneNumber: string;
}

export interface PosterLayer {
  id: string;
  x: number;
  y: number;
  text: string;
}